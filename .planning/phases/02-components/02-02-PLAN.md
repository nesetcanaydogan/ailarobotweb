---
phase: 02-components
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [apps/web/src/components/sections/Hero.tsx, apps/web/src/components/forms/LeadForm.tsx]
autonomous: true
requirements: [COMP-05, COMP-06]
user_setup: []

must_haves:
  truths:
    - "Hero section renders with Lucide ArrowRight and Play icons"
    - "LeadForm uses React 19 useActionState for submission"
    - "LeadForm successfully calls the backend API and handles validation errors"
  artifacts:
    - path: "apps/web/src/components/sections/Hero.tsx"
      provides: "Main landing section with CTAs"
    - path: "apps/web/src/components/forms/LeadForm.tsx"
      provides: "Interactive lead capture form"
  key_links:
    - from: "apps/web/src/components/forms/LeadForm.tsx"
      to: "/api/leads"
      via: "fetch in form action"
    - from: "apps/web/src/components/sections/Hero.tsx"
      to: "apps/web/src/components/ui/Button.tsx"
      via: "component import"
---

<objective>
Implement the primary interactive sections of the AILA website: the Hero section and the Lead capture form. This wires the UI components to real backend logic and establishes the content hierarchy.

Purpose: Engage users with high-impact visuals and capture leads for the AILA robot.
Output: Fully functional Hero section and LeadForm integrated with the API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-components/02-RESEARCH.md
@.planning/phases/02-components/02-VALIDATION.md
@DESIGN.md
@.planning/phases/02-components/02-01-PLAN.md
@apps/web/src/index.css
</context>

<interfaces>
From apps/web/src/components/ui/Button.tsx:
```typescript
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'outline' | 'solid';
}
export function Button({ variant, children, ...props }: ButtonProps): JSX.Element;
```

From apps/web/src/components/ui/Input.tsx:
```typescript
export interface InputProps extends React.InputHTMLAttributes<HTMLInputElement> {}
export function Input(props: InputProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hero Section Implementation (Wave 2)</name>
  <files>apps/web/src/components/sections/Hero.tsx</files>
  <action>
    - Create `apps/web/src/components/sections/Hero.tsx`:
      - Implement a centered layout using `flex flex-col items-center text-center`.
      - Headline: "The First Truly Autonomous Robotic Companion". Use `font-aeonikpro` and `text-display-xl`.
      - Subheadline: "AILA combines advanced RAG, physical interaction, and modular intelligence...". Use `font-untitled-sans` and `text-comet`.
      - CTA Buttons:
        - "Get Started" using `Button` (solid) with `ArrowRight` icon from `lucide-react`.
        - "Watch Demo" using `Button` (outline) with `Play` icon from `lucide-react`.
      - Background: Use the `Midnight Abyss` background and the glassy layout shell.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/sections/Hero.test.tsx</automated>
  </verify>
  <done>Hero section is implemented with correct icons, typography, and layout.</done>
</task>

<task type="auto">
  <name>Task 2: LeadForm Implementation (Wave 2)</name>
  <files>apps/web/src/components/forms/LeadForm.tsx</files>
  <action>
    - Create `apps/web/src/components/forms/LeadForm.tsx`:
      - Use React 19 `useActionState` to handle form submission.
      - Use `LeadSchema` from `@aila/shared` for validation.
      - Action `submitLead`:
        - Extracts `email` and `businessName` from `FormData`.
        - Calls `POST /api/leads`.
        - Returns success state or field-level errors.
      - UI:
        - Wrap in a `GlassyCard`.
        - Use `Input` components for Email and Business Name.
        - Use `Button` (solid) for the "Request Demo" submit button.
        - Display error messages from the action state.
        - Show a success message upon successful submission.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/forms/LeadForm.test.tsx</automated>
  </verify>
  <done>LeadForm is implemented with React 19 Actions and integrated with the backend API.</done>
</task>

</tasks>

<success_criteria>
- Hero section renders with Lucide icons and correct typography.
- LeadForm successfully submits and handles both success and error states (validation/duplicate).
- All components pass their respective Vitest tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-components/02-02-SUMMARY.md`
</output>
