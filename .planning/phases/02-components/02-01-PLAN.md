---
phase: 02-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/package.json, apps/web/vitest.config.ts, apps/web/src/test/setup.ts, apps/web/src/components/ui/Button.tsx, apps/web/src/components/ui/Button.test.tsx, apps/web/src/components/cards/GlassyCard.tsx, apps/web/src/components/cards/GlassyCard.test.tsx, apps/api/src/index.ts, apps/api/src/routes/leads.ts]
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
user_setup: []

must_haves:
  truths:
    - "Vitest environment is configured and running in apps/web"
    - "All buttons use rounded-full (Pill) radius regardless of variant"
    - "GlassyCard exhibits the multi-layer shadow-glass effect"
    - "API endpoint POST /api/leads validates data against LeadSchema"
  artifacts:
    - path: "apps/web/vitest.config.ts"
      provides: "Test runner configuration"
    - path: "apps/web/src/components/ui/Button.tsx"
      provides: "Atomic UI component (strict Pill radius)"
    - path: "apps/web/src/components/cards/GlassyCard.tsx"
      provides: "Frosted glass container component"
    - path: "apps/api/src/routes/leads.ts"
      provides: "Lead capture API endpoint"
  key_links:
    - from: "apps/web/src/components/ui/Button.tsx"
      to: "Tailwind v4 theme"
      via: "rounded-full class"
    - from: "apps/api/src/routes/leads.ts"
      to: "@aila/shared"
      via: "LeadSchema.safeParse"
---

<objective>
Setup the testing environment and implement foundational UI components and the backend lead capture API. 

Purpose: Establish the 'Internal Dashboard' aesthetic and enable data persistence while ensuring quality through automated testing.
Output: Configured Vitest environment, foundational UI atoms (Button, GlassyCard), and a functional lead capture endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-components/02-RESEARCH.md
@.planning/phases/02-components/02-VALIDATION.md
@DESIGN.md
@apps/web/src/index.css
@apps/api/prisma/schema.prisma
@packages/shared/src/schemas/lead.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Test Environment Setup (Wave 0)</name>
  <files>apps/web/package.json, apps/web/vitest.config.ts, apps/web/src/test/setup.ts</files>
  <action>
    - Install testing dependencies in `apps/web`: `npm install -D vitest @vitejs/plugin-react @testing-library/react @testing-library/dom jsdom`.
    - Install UI utilities in `apps/web`: `npm install framer-motion clsx tailwind-merge`.
    - Create `apps/web/vitest.config.ts` configured for `jsdom` and React plugin.
    - Create `apps/web/src/test/setup.ts` to include `@testing-library/jest-dom` matchers.
    - Add `test: "vitest"` script to `apps/web/package.json`.
    - Create placeholder test files for Wave 1 and Wave 2 components to satisfy VALIDATION.md map:
      - `apps/web/src/components/ui/Button.test.tsx`
      - `apps/web/src/components/ui/Input.test.tsx`
      - `apps/web/src/components/cards/GlassyCard.test.tsx`
      - `apps/web/src/components/sections/Hero.test.tsx`
      - `apps/web/src/components/forms/LeadForm.test.tsx`
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run --passWithNoTests</automated>
  </verify>
  <done>Vitest is configured and all placeholder test files exist.</done>
</task>

<task type="auto">
  <name>Task 1: Foundational UI Components (Wave 1)</name>
  <files>apps/web/src/components/ui/Button.tsx, apps/web/src/components/cards/GlassyCard.tsx, apps/web/src/components/ui/Input.tsx</files>
  <action>
    - Create `apps/web/src/components/ui/Button.tsx`:
      - Implement variants: `primary` (bg-white/6), `outline` (border-azure-glow/12), `solid` (bg-neon-violet).
      - **CRITICAL (D-01):** ALL variants MUST use `rounded-full` (Pill radius) to comply with the "Don't" rule in DESIGN.md.
      - Use `clsx` and `tailwind-merge` for styling.
    - Create `apps/web/src/components/cards/GlassyCard.tsx`:
      - Implement the "Internal Dashboard" card style.
      - Apply `bg-[rgba(186,214,247,0.03)]`, `backdrop-blur-xl`, and `shadow-glass`.
      - Use `border-white/10`.
    - Create `apps/web/src/components/ui/Input.tsx`:
      - "Minimal Input Field" style: `bg-azure-glow/6`, `border-white/20`, `text-white`, `rounded-lg`.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/ui/Button.test.tsx src/components/cards/GlassyCard.test.tsx</automated>
  </verify>
  <done>Button (strict Pill radius), GlassyCard, and Input components are implemented and pass unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Lead Capture API Endpoint (Wave 1)</name>
  <files>apps/api/src/routes/leads.ts, apps/api/src/index.ts</files>
  <action>
    - Create `apps/api/src/routes/leads.ts`:
      - Implement POST `/` (relative to `/api/leads`).
      - Use `LeadSchema.safeParse` from `@aila/shared` for validation.
      - Use Prisma Client to save data to the `Lead` model.
      - Return 201 on success, 400 on validation error, 409 on duplicate email.
    - Update `apps/api/src/index.ts`:
      - Import and mount the leads router at `/api/leads`.
  </action>
  <verify>
    <automated>grep "LeadSchema.safeParse" apps/api/src/routes/leads.ts && grep "/api/leads" apps/api/src/index.ts</automated>
  </verify>
  <done>API endpoint is functional and integrated with shared validation and database.</done>
</task>

</tasks>

<success_criteria>
- `npx vitest run` executes successfully in `apps/web`.
- Button component uses `rounded-full` for all variants.
- POST `/api/leads` successfully persists data (verified via curl or manual test).
</success_criteria>

<output>
After completion, create `.planning/phases/02-components/02-01-SUMMARY.md`
</output>
