---
phase: 03-3d
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [apps/web/src/components/3d/Controller.tsx, apps/web/src/components/3d/Scene.tsx, apps/web/src/components/3d/AilaPlaceholder.tsx]
autonomous: true
requirements: [3D-01, 3D-02]
must_haves:
  truths:
    - "AILA robot moves, rotates, and scales in sync with scroll position"
    - "AILA passes behind some cards and in front of others (z-index checks)"
    - "Atmospheric Azure and Celestial lighting reacts to AILA's position"
  artifacts:
    - path: "apps/web/src/components/3d/Controller.tsx"
      provides: "Scroll-synced GSAP animation logic"
  key_links:
    - from: "apps/web/src/components/3d/Scene.tsx"
      to: "apps/web/src/components/3d/Controller.tsx"
      via: "Component embedding"
---

<objective>
Bring the AILA robot to life with cinematic, scroll-synced animations and fine-tuned visual interactions. This plan implements the logic that drives AILA's movement across the narrative sections and optimizes its relationship with the UI layers.

Purpose: Create an immersive, premium "Internal Dashboard" experience.
Output: Smooth, scroll-driven robot animations with dynamic lighting and correct layering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-3d/03-RESEARCH.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Scroll-Synced GSAP Controller</name>
  <files>apps/web/src/components/3d/Controller.tsx, apps/web/src/components/3d/Scene.tsx</files>
  <action>
    - Create `Controller.tsx` using `useScroll` and `useFrame` from R3F.
    - Setup a `gsap` timeline in a `useLayoutEffect` that defines AILA's path:
      - Section 1 (Hero): Center, idle breathing.
      - Section 2 (About): Move left, rotate 45 deg.
      - Section 3 (Verticals): Move right, scale down slightly.
      - Section 4 (How it Works): Move to bottom right, rotate to show "sensors".
    - In `useFrame`, seek the timeline based on `scroll.offset`.
    - Integrate `Controller` into `Scene.tsx` so it can control the `AilaPlaceholder` via refs.
  </action>
  <verify>
    <automated>grep "useScroll" apps/web/src/components/3d/Controller.tsx && grep "gsap.timeline" apps/web/src/components/3d/Controller.tsx</automated>
  </verify>
  <done>AILA movement is strictly synced to scroll progress using a GSAP timeline.</done>
</task>

<task type="auto">
  <name>Task 2: Atmosphere & Z-index Tuning</name>
  <files>apps/web/src/components/3d/Scene.tsx, apps/web/src/components/3d/AilaPlaceholder.tsx</files>
  <action>
    - Add atmospheric lighting to `Scene.tsx`:
      - `PointLight` with `Celestial Light` (#b6d9fc) that follows AILA.
      - `RectAreaLight` or similar for soft highlights on the "robot shell".
    - Fine-tune Z-index interactions:
      - Use `RenderOrder` or layered components in R3F if needed to ensure AILA appears *behind* the translucent `GlassyCards` in the Verticals section but *on top* of the background gradients.
      - Add subtle glows (using Drei's `Float` or `Sparkles` if appropriate for "Edge AI" feel).
  </action>
  <verify>
    <automated>grep "intensity" apps/web/src/components/3d/Scene.tsx && grep "color" apps/web/src/components/3d/Scene.tsx</automated>
  </verify>
  <done>AILA interacts visually with UI layers and the atmosphere matches DESIGN.md.</done>
</task>

</tasks>

<success_criteria>
- AILA moves predictably through the defined sections.
- Movement is smooth (no jitter) due to ScrollControls damping.
- Lighting colors (Azure/Celestial) are clearly visible and provide depth to the placeholder model.
</success_criteria>
