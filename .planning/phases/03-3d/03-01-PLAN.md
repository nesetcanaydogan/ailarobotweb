---
phase: 03-3d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/package.json, apps/web/src/test/setup.ts, apps/web/src/components/3d/Scene.tsx, apps/web/src/components/3d/AilaPlaceholder.tsx, apps/web/src/App.tsx]
autonomous: true
requirements: [3D-01, 3D-03]
must_haves:
  truths:
    - "Three.js Canvas renders on the landing page"
    - "AILA placeholder model is visible and centers in the viewport"
    - "ScrollControls damping allows for smooth inertial scrolling"
  artifacts:
    - path: "apps/web/src/components/3d/Scene.tsx"
      provides: "R3F Canvas & ScrollControls setup"
    - path: "apps/web/src/components/3d/AilaPlaceholder.tsx"
      provides: "Initial 3D representation of AILA"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/components/3d/Scene.tsx"
      via: "Component embedding"
---

<objective>
Setup the foundational 3D environment and scroll orchestration using React Three Fiber and Drei. This plan establishes the "Sandwich" architecture where the 3D model lives in a fixed background layer while the HTML content scrolls over/under it.

Purpose: Enable cinematic 3D storytelling via scroll-driven interactions.
Output: A working 3D Canvas with a placeholder robot and functional scroll damping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-3d/03-RESEARCH.md
@DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install 3D Dependencies & Setup Test Mock</name>
  <files>apps/web/package.json, apps/web/src/test/setup.ts</files>
  <action>
    - Install core 3D libraries: `three`, `@react-three/fiber`, `@react-three/drei`, `gsap`, `react-router-dom`.
    - Update `apps/web/src/test/setup.ts` to mock `HTMLCanvasElement.getContext` and `ResizeObserver` to prevent Vitest crashes during 3D component testing.
    - Add `@types/three` to devDependencies.
  </action>
  <verify>
    <automated>cd apps/web && npm install && npm test</automated>
  </verify>
  <done>Dependencies installed and test environment supports Canvas components.</done>
</task>

<task type="auto">
  <name>Task 2: Create Scene & AilaPlaceholder</name>
  <files>apps/web/src/components/3d/Scene.tsx, apps/web/src/components/3d/AilaPlaceholder.tsx</files>
  <action>
    - Create `AilaPlaceholder.tsx` using R3F primitives (e.g., a Sphere for head, Box for body) to represent the robot's volume.
    - Create `Scene.tsx` which exports a `Scene` component containing:
      - `<Canvas>` with high pixel ratio and alpha enabled.
      - `<ScrollControls>` with `pages={6}` and `damping={0.2}` as researched.
      - `<AilaPlaceholder />` positioned at the center.
      - Lights: `AmbientLight` (intensity 0.5) and `SpotLight` (Azure Glow: #c7d3ea).
  </action>
  <verify>
    <automated>npm test -- apps/web/src/components/3d/Scene.tsx</automated>
  </verify>
  <done>3D Scene with placeholder model exists and renders without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Sandwich Architecture</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    - Update `App.tsx` to wrap the main content in the `Scene` component.
    - Use `<Scroll html>` from Drei to contain the existing UI sections (Hero, etc.).
    - Ensure the Canvas is fixed in the background (z-index: 10) while the background color (`midnight-abyss`) remains at z-0.
    - Content inside `<Scroll html>` should be at z-20 to appear over AILA initially.
  </action>
  <verify>
    <automated>grep "Scroll" apps/web/src/App.tsx && grep "Canvas" apps/web/src/components/3d/Scene.tsx</automated>
  </verify>
  <done>App structure supports 3D background with HTML overlay via ScrollControls.</done>
</task>

</tasks>

<success_criteria>
- AILA placeholder is visible on the home page.
- Scrolling the page moves the "ghost" scroll of ScrollControls (can be verified via console log in useFrame).
- UI sections (Hero) are still interactive and visible over the 3D model.
</success_criteria>
