---
phase: 04-demo
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [apps/web/src/components/demo/DemoResult.tsx, apps/web/src/components/demo/ConfigViewer.tsx, apps/web/src/App.tsx]
autonomous: true
requirements: [1.2]
---

<objective>
Visualize the generated AI configuration and add final polish to the demo experience.

Purpose: Provide a tangible, high-value output from the demo that reinforces the brand promise.
Output: An interactive results dashboard and a "Reset Demo" flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-demo/04-RESEARCH.md
@apps/web/src/hooks/useAilaDemo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DemoResult Dashboard and ConfigViewer</name>
  <files>apps/web/src/components/demo/DemoResult.tsx, apps/web/src/components/demo/ConfigViewer.tsx</files>
  <action>
    - Create `DemoResult` component as a full-screen or large modal-style dashboard.
    - Implement `ConfigViewer` to visualize the JSON data (Personality, Tasks, Skills).
    - Use glassy cards, dotDigital typography, and interactive hover states.
    - Include sections for: "Mission Profile", "Hardware Modules", and "Behavioral Matrix".
    - Use `framer-motion` to animate the entrance of each dashboard section.
  </action>
  <verify>
    <automated>npm run build --prefix apps/web</automated>
  </verify>
  <done>High-fidelity results dashboard that presents the AI-generated AILA configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Add Reset Flow and UI/UX Transitions</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/demo/DemoResult.tsx</files>
  <action>
    - Add a "Configure New Mission" (Reset) button to the `DemoResult` view.
    - Ensure resetting clears all demo states and returns the user to the `LeadForm` (or a simplified version for repeat demos).
    - Implement smooth cross-fade transitions between `LeadForm` -> `AgenticProgress` -> `DemoResult`.
    - Ensure all components follow the 'Midnight Abyss' aesthetic with subtle atmospheric glows.
  </action>
  <verify>
    <automated>npm run build --prefix apps/web</automated>
  </verify>
  <done>Polished, end-to-end demo flow with reset capability and smooth transitions.</done>
</task>

</tasks>

<verification>
- [ ] End-to-end demo flow works: Lead -> Progress -> Result.
- [ ] Reset button correctly returns user to start state.
- [ ] UI follows all design tokens and aesthetic requirements.
</verification>

<success_criteria>
A fully functional, cinematic agentic demo that converts leads and demonstrates AILA's adaptive intelligence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-03-SUMMARY.md`
</output>
