---
phase: 04-demo
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [
  "apps/web/src/components/demo/DemoResult.tsx",
  "apps/web/src/App.tsx",
  "apps/web/src/components/forms/LeadForm.tsx"
]
autonomous: true
requirements: [REQ-1.2, REQ-1.3]

must_haves:
  truths:
    - "Final AilaConfig is displayed in a glassy dashboard after generation"
    - "Submitting LeadForm triggers the Demo flow (Progress -> Result)"
    - "Transitions between LeadForm, Progress, and Result are smooth"
  artifacts:
    - path: "apps/web/src/components/demo/DemoResult.tsx"
      provides: "Final configuration dashboard"
    - path: "apps/web/src/App.tsx"
      provides: "Unified demo state machine"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "LeadForm"
      via: "onSuccess callback with leadId"
    - from: "apps/web/src/App.tsx"
      to: "useAilaDemo"
      via: "startDemo call"
---

<objective>
Complete the Phase 4 goals by implementing the final result visualization dashboard and integrating the full demo flow into the main application.

Purpose: Provide a satisfying "payoff" for the user after the agentic generation process.
Output: High-fidelity configuration dashboard and integrated demo flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-demo/04-RESEARCH.md
@.planning/phases/04-demo/04-VALIDATION.md
@.planning/phases/04-demo/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: DemoResult Dashboard (Wave 3)</name>
  <files>apps/web/src/components/demo/DemoResult.tsx</files>
  <action>
    1. Create `apps/web/src/components/demo/DemoResult.tsx`.
    2. Use the "Internal Dashboard" aesthetic (glassy cards, subtle borders, high contrast).
    3. Display the final `AilaConfig`:
       - Persona section with name, role, and traits.
       - Tasks list with priority indicators.
       - Modules grid showing status and type.
    4. Include a "Download Config" or "Share" button (mocked or simple JSON copy).
    5. Ensure responsiveness and use dotDigital fonts (Inter/Space Grotesk).
  </action>
  <verify>
    <automated>npm test -- --filter=DemoResult</automated>
  </verify>
  <done>Final configuration dashboard implemented with glassy aesthetic.</done>
</task>

<task type="auto">
  <name>Task 1: Full Flow Integration (Wave 3)</name>
  <files>apps/web/src/App.tsx, apps/web/src/components/forms/LeadForm.tsx</files>
  <action>
    1. Update `apps/web/src/components/forms/LeadForm.tsx` to accept an `onSuccess` callback that passes the `leadId`.
    2. In `apps/web/src/App.tsx`:
       - Manage `demoState`: 'lead-capture' | 'generating' | 'result'.
       - Implement state transitions:
         - `LeadForm` success -> call `startDemo(leadId)` -> set state to 'generating'.
         - `useAilaDemo` completion -> set state to 'result'.
       - Render the appropriate component based on `demoState`.
    3. Add entry/exit animations for each state using `AnimatePresence`.
  </action>
  <verify>
    <automated>npx vitest apps/web/tests/demo_flow.spec.ts (create if missing)</automated>
  </verify>
  <done>Full demo flow integrated and functional from lead capture to result visualization.</done>
</task>

</tasks>

<verification>
Perform the "End-to-End Demo Flow" manual verification as defined in 04-VALIDATION.md.
</verification>

<success_criteria>
- User can complete the entire journey: Enter info -> Watch generation -> See result.
- All configurations are persisted in the backend and linked to the lead.
- The UI feels snappy, intelligent, and premium.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-03-SUMMARY.md`
</output>
