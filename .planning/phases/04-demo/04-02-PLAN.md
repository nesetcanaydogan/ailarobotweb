---
phase: 04-demo
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [apps/web/src/hooks/useAilaDemo.ts, apps/web/src/components/demo/AgenticProgress.tsx, apps/web/src/components/forms/LeadForm.tsx]
autonomous: true
requirements: [1.2]
---

<objective>
Implement the frontend streaming logic and progress visualization for the agentic demo.

Purpose: Provide real-time feedback and "agentic" feel during the AI generation process.
Output: Custom hook for SSE consumption and an animated progress UI.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-demo/04-RESEARCH.md
@apps/web/src/components/forms/LeadForm.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useAilaDemo Hook</name>
  <files>apps/web/src/hooks/useAilaDemo.ts, apps/web/src/hooks/useAilaDemo.test.ts</files>
  <behavior>
    - `startDemo(industry: string)` should initiate SSE connection to `/api/demo/stream`.
    - It should parse incoming chunks and update `status` and `result` state.
    - It should handle stream termination ([DONE]) and errors.
    - It should expose `isStreaming`, `progressSteps`, and `finalConfig`.
  </behavior>
  <action>
    - Implement `useAilaDemo` using `EventSource` (or `fetch` with readable stream if preferred for headers).
    - Manage state for current reasoning step and accumulated JSON.
    - Parse "agentic steps" from the stream to display progress.
  </action>
  <verify>
    <automated>npx vitest apps/web/src/hooks/useAilaDemo.test.ts</automated>
  </verify>
  <done>Custom hook for managing demo streaming state and parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AgenticProgress UI Component</name>
  <files>apps/web/src/components/demo/AgenticProgress.tsx</files>
  <action>
    - Create a component to visualize the "thinking" process of AILA.
    - Use `framer-motion` for smooth transitions between steps (e.g., "Analyzing Vertical", "Selecting Modules", "Calibrating Personality").
    - Use the 'Internal Dashboard' aesthetic (Midnight Abyss, Glassy cards, subtle glows).
    - Display current status badges and an animated progress bar.
  </action>
  <verify>
    <automated>npm run build --prefix apps/web</automated>
  </verify>
  <done>Animated progress UI that visually communicates the AI's reasoning steps.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Demo Flow into LeadForm</name>
  <files>apps/web/src/components/forms/LeadForm.tsx</files>
  <action>
    - Modify `LeadForm` to transition to the demo flow after successful lead capture.
    - Pass the captured `businessName` or a new "Industry/Requirement" field to the `useAilaDemo` hook.
    - Replace the "Request Received" success state with the `AgenticProgress` component.
  </action>
  <verify>
    <automated>npx vitest apps/web/src/components/forms/LeadForm.test.tsx</automated>
  </verify>
  <done>Lead capture now triggers the agentic demo experience.</done>
</task>

</tasks>

<verification>
- [ ] `useAilaDemo` hook correctly parses stream chunks in tests.
- [ ] `LeadForm` transitions to progress state on success.
- [ ] UI builds without errors.
</verification>

<success_criteria>
The user experience transitions from lead capture to a live, streaming AI "configuration" process.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-02-SUMMARY.md`
</output>
