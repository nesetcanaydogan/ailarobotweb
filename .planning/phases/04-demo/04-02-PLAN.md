---
phase: 04-demo
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [
  "apps/web/src/hooks/useAilaDemo.ts",
  "apps/web/src/components/demo/AgenticProgress.tsx"
]
autonomous: true
requirements: [REQ-1.2]

must_haves:
  truths:
    - "useAilaDemo hook correctly parses SSE stream chunks into AilaConfig state"
    - "useAilaDemo handles request abort via AbortController"
    - "AgenticProgress component animates through generation steps"
  artifacts:
    - path: "apps/web/src/hooks/useAilaDemo.ts"
      provides: "Demo streaming logic"
    - path: "apps/web/src/components/demo/AgenticProgress.tsx"
      provides: "Visual feedback during generation"
  key_links:
    - from: "apps/web/src/hooks/useAilaDemo.ts"
      to: "/api/demo/stream"
      via: "fetch with SSE processing"
---

<objective>
Implement the frontend logic to consume the streaming demo API and provide visual feedback to the user during the agentic generation process.

Purpose: Deliver a low-latency, "intelligent" feel by rendering data as it arrives.
Output: Custom hook for SSE consumption and a Framer Motion animated progress component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-demo/04-RESEARCH.md
@.planning/phases/04-demo/04-VALIDATION.md
@.planning/phases/04-demo/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: useAilaDemo Hook (Wave 2)</name>
  <files>apps/web/src/hooks/useAilaDemo.ts</files>
  <behavior>
    - calling startDemo(leadId) initiates fetch to /api/demo/stream
    - state updates as chunks arrive (partial JSON parsing if needed)
    - handles errors and termination ([DONE])
    - exposes stopDemo() which aborts the fetch
  </behavior>
  <action>
    1. Create `apps/web/src/hooks/useAilaDemo.ts`.
    2. Use `useState` to track: `status` ('idle', 'streaming', 'completed', 'error'), `config` (Partial<AilaConfig>), and `error`.
    3. Implement `startDemo(leadId: string)`:
       - Use `AbortController`.
       - Fetch `/api/demo/stream?leadId=${leadId}`.
       - Use `ReadableStreamDefaultReader` to process chunks.
       - Parse SSE format (`data: {...}`).
       - Update `config` state incrementally.
    4. Ensure cleanup of the controller on unmount or manual stop.
  </action>
  <verify>
    <automated>vitest apps/web/src/hooks/useAilaDemo.test.ts</automated>
  </verify>
  <done>Hook functional and tested against mocked SSE stream.</done>
</task>

<task type="auto">
  <name>Task 1: AgenticProgress UI Component (Wave 2)</name>
  <files>apps/web/src/components/demo/AgenticProgress.tsx</files>
  <action>
    1. Create `apps/web/src/components/demo/AgenticProgress.tsx`.
    2. Define steps based on `AilaConfig` sections (Persona, Tasks, Modules).
    3. Use `framer-motion` for transitions between steps.
    4. Display status badges (e.g., "Designing Persona...", "Synthesizing Modules...") with pulsing animations.
    5. Show a progress bar or indicator that fills as sections of the config arrive.
  </action>
  <verify>
    <automated>npm test -- --filter=AgenticProgress</automated>
  </verify>
  <done>Animated progress UI provides real-time feedback during demo generation.</done>
</task>

</tasks>

<verification>
Reference 04-VALIDATION.md for streaming UI verification steps.
</verification>

<success_criteria>
- Hook correctly handles incremental state updates from SSE.
- UI provides a cinematic, high-end feel consistent with the rest of the site.
- Abort logic is verified to prevent memory leaks in the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-02-SUMMARY.md`
</output>
