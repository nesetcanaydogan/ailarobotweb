---
phase: 04-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/api/package.json, apps/api/src/index.ts, apps/api/prisma/schema.prisma, apps/api/src/routes/demo.ts, apps/api/src/lib/openai.ts, apps/api/vitest.config.ts]
autonomous: true
requirements: [1.2, 1.3, 2.3]
user_setup:
  - service: openai
    why: "Personalized AILA demo generation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys"

must_haves:
  truths:
    - "Backend can connect to OpenAI API using server-side key"
    - "GET /api/demo/stream returns a valid text/event-stream"
    - "Database schema includes DemoResult linked to Lead"
  artifacts:
    - path: "apps/api/src/routes/demo.ts"
      provides: "SSE Proxy endpoint"
    - path: "apps/api/prisma/schema.prisma"
      provides: "DemoResult model"
  key_links:
    - from: "apps/api/src/routes/demo.ts"
      to: "OpenAI SDK"
      via: "openai.chat.completions.create"
---

<objective>
Setup the backend infrastructure for the agentic demo, including OpenAI integration, streaming proxy, and database schema updates.

Purpose: Enable secure, low-latency streaming of AI-generated configurations.
Output: Working SSE endpoint and updated database model.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-demo/04-RESEARCH.md
@apps/api/prisma/schema.prisma
@apps/api/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Backend Testing and OpenAI SDK</name>
  <files>apps/api/package.json, apps/api/vitest.config.ts, apps/api/src/lib/openai.ts</files>
  <action>
    - Install `openai` and `express-rate-limit` in `apps/api`.
    - Install `vitest` as a dev dependency.
    - Create `apps/api/vitest.config.ts` (base config for node testing).
    - Create `apps/api/src/lib/openai.ts` to initialize the OpenAI client using `OPENAI_API_KEY` from env.
    - Add a `test` script to `apps/api/package.json`.
  </action>
  <verify>
    <automated>cd apps/api && npm install && npm test</automated>
  </verify>
  <done>Dependencies installed, testing framework ready, and OpenAI client initialized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GPT-4o SSE Proxy Route</name>
  <files>apps/api/src/routes/demo.ts, apps/api/src/index.ts, apps/api/src/routes/demo.test.ts</files>
  <behavior>
    - GET /api/demo/stream should return headers: Content-Type: text/event-stream, Cache-Control: no-cache, Connection: keep-alive.
    - It should stream chunks from OpenAI and end with [DONE].
    - It should implement rate limiting (max 5 requests per minute per IP).
  </behavior>
  <action>
    - Create `apps/api/src/routes/demo.ts` with an SSE endpoint.
    - Use `express-rate-limit` to protect the route.
    - Use the OpenAI SDK's streaming feature to proxy chunks to the client.
    - Ensure `res.flushHeaders()` is called for immediate streaming.
    - Register the route in `apps/api/src/index.ts`.
  </action>
  <verify>
    <automated>npx vitest apps/api/src/routes/demo.test.ts</automated>
  </verify>
  <done>Functional SSE proxy endpoint with rate limiting and tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update Prisma Schema for Demo Results</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
    - Add `DemoResult` model to `schema.prisma`.
    - Fields: id (UUID), leadId (relation to Lead), config (Json - stores generated personality/modules), industry (String), createdAt.
    - Add `results DemoResult[]` to `Lead` model.
    - Run `npx prisma generate`.
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Prisma schema updated with DemoResult model and relationships.</done>
</task>

</tasks>

<verification>
- [ ] `npm test` passes in `apps/api`.
- [ ] `npx prisma validate` passes.
- [ ] Manual check of `curl -N http://localhost:3001/api/demo/stream` (once env key is set).
</verification>

<success_criteria>
Backend is ready to handle demo requests, stream AI responses, and store results in the database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-01-SUMMARY.md`
</output>
