---
phase: 04-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "packages/shared/src/schemas/demo.ts",
  "packages/shared/src/index.ts",
  "apps/api/package.json",
  "apps/api/vitest.config.ts",
  "apps/api/prisma/schema.prisma",
  "apps/api/src/routes/demo.ts",
  "apps/api/src/index.ts"
]
autonomous: true
requirements: [REQ-1.2, REQ-1.3, REQ-2.3]
user_setup:
  - service: openai
    why: "GPT-4o configuration generation"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard"

must_haves:
  truths:
    - "Shared AilaConfigSchema validates persona, tasks, and modules"
    - "API endpoint /api/demo/stream returns text/event-stream"
    - "Aborting SSE request triggers AbortController cleanup"
    - "Final LLM output is persisted to PostgreSQL linked to Lead"
  artifacts:
    - path: "packages/shared/src/schemas/demo.ts"
      provides: "AilaConfig Zod schema"
    - path: "apps/api/src/routes/demo.ts"
      provides: "SSE Proxy with OpenAI integration"
    - path: "apps/api/prisma/schema.prisma"
      provides: "DemoConfig model"
  key_links:
    - from: "apps/api/src/routes/demo.ts"
      to: "OpenAI SDK"
      via: "streaming chat completion"
    - from: "apps/api/src/routes/demo.ts"
      to: "Prisma"
      via: "DemoConfig.create"
---

<objective>
Setup the backend infrastructure for the agentic demo, including shared contracts, API testing environment, and the streaming proxy with persistence.

Purpose: Bridge marketing content with agentic functionality while ensuring security and resource efficiency.
Output: Shared schemas, Prisma migration, and a functional SSE endpoint with GPT-4o integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-demo/04-RESEARCH.md
@.planning/phases/04-demo/04-VALIDATION.md
@apps/api/prisma/schema.prisma
@packages/shared/src/schemas/lead.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Shared Contracts & Test Setup (Wave 0)</name>
  <files>packages/shared/src/schemas/demo.ts, packages/shared/src/index.ts, apps/api/package.json, apps/api/vitest.config.ts</files>
  <action>
    1. Create `packages/shared/src/schemas/demo.ts` with `AilaConfigSchema` containing:
       - `persona`: { name, role, traits: string[] }
       - `tasks`: { id, description, priority: 'low'|'medium'|'high' }[]
       - `modules`: { id, name, type, status: 'active'|'standby'|'offline' }[]
    2. Export `AilaConfigSchema` and `AilaConfig` type from `packages/shared/src/index.ts`.
    3. Add `vitest`, `openai`, `express-rate-limit` to `apps/api/package.json`.
    4. Create `apps/api/vitest.config.ts` for backend testing (environment: node).
    5. Setup Vitest mocks for SSE and OpenAI in `apps/api/src/test/setup.ts` (if needed) or directly in tests.
  </action>
  <verify>
    <automated>npm test -w packages/shared && ls apps/api/vitest.config.ts</automated>
  </verify>
  <done>Shared schemas available and API test environment ready.</done>
</task>

<task type="auto">
  <name>Task 1: Prisma Schema & Persistence (Wave 1)</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>
    1. Update `apps/api/prisma/schema.prisma` to include a `DemoConfig` model:
       - `id`: String (UUID) @id
       - `config`: Json (Matches AilaConfigSchema)
       - `leadId`: String @unique
       - `lead`: Lead @relation(fields: [leadId], references: [id])
       - `createdAt`: DateTime @default(now())
    2. Update `Lead` model to have an optional relation to `DemoConfig`.
    3. Run `npx prisma migrate dev --name add_demo_config`.
  </action>
  <verify>
    <automated>npx prisma validate && npx prisma migrate status</automated>
  </verify>
  <done>Database schema updated to support demo persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSE Proxy with Cleanup & Persistence (Wave 1)</name>
  <files>apps/api/src/routes/demo.ts, apps/api/src/index.ts</files>
  <behavior>
    - GET /api/demo/stream?leadId=xxx returns 200 with text/event-stream
    - Request emits chunks as they arrive from OpenAI
    - Final chunk triggers DB persistence of the full config
    - Closing the request calls controller.abort() (SSE Cleanup) per 04-VALIDATION.md
  </behavior>
  <action>
    1. Implement `apps/api/src/routes/demo.ts` using `express.Router`.
    2. Add `GET /stream` endpoint:
       - Validate `leadId` exists.
       - Setup `AbortController`.
       - Listen for `req.on('close')` to abort the controller.
       - Use OpenAI SDK with `stream: true` and pass the abort signal.
       - Stream valid SSE chunks (data: JSON.stringify(chunk)).
       - On stream end, validate full config with `AilaConfigSchema` and save to `DemoConfig` table.
    3. Register the router in `apps/api/src/index.ts` under `/api/demo`.
    4. Apply `express-rate-limit` to this route to prevent abuse.
  </action>
  <verify>
    <automated>vitest apps/api/src/routes/demo.test.ts</automated>
  </verify>
  <done>Streaming API functional with proper cleanup and persistence.</done>
</task>

</tasks>

<verification>
Reference 04-VALIDATION.md for full automated verification of SSE integrity and persistence.
</verification>

<success_criteria>
- Backend proxy handles client disconnects gracefully (SSE Cleanup).
- Demo configurations are correctly stored in PostgreSQL.
- API keys are never exposed to the client.
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo/04-01-SUMMARY.md`
</output>
